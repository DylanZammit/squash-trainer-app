# Copy this file to .env and fill in real values before running the backend.
#
# The backend is only needed for local web development.
# The production Android app talks directly to Supabase (no backend required).

JWT_SECRET=change-this-to-a-long-random-string
PORT=3000
# DB_PATH=./squash.db   # override SQLite path (set automatically by docker-compose)
