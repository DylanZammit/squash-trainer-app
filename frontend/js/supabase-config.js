/**
 * Supabase project credentials.
 *
 * HOW TO FILL THESE IN:
 *   1. Go to https://supabase.com/dashboard
 *   2. Open your project → Settings → API
 *   3. Copy "Project URL"  → paste as SUPABASE_URL below
 *   4. Copy "anon / public" key → paste as SUPABASE_ANON_KEY below
 *
 * The anon key is safe to ship in a mobile app — Row Level Security
 * policies ensure each user can only access their own data.
 */

// eslint-disable-next-line no-unused-vars
const SUPABASE_URL      = 'https://YOUR_PROJECT_ID.supabase.co';
// eslint-disable-next-line no-unused-vars
const SUPABASE_ANON_KEY = 'YOUR_ANON_PUBLIC_KEY_HERE';
