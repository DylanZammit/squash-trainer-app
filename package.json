{
  "name": "squash-trainer",
  "version": "1.0.0",
  "description": "Squash training app — Capacitor Android build",
  "private": true,
  "scripts": {
    "sync":    "cap sync android",
    "open":    "cap open android",
    "build":   "cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core":    "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
